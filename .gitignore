node_modules/
dist/
contexts-data/

# Local MCP server registrations (may contain auth tokens/cookies)
.mcp.json

# Local Claude Code settings (may contain machine-specific paths and secrets)
.claude/
